// Backend base URL (Vercel / Render / Local)
export const serverUrl = import.meta.env.VITE_BACKEND_URL;

// Optional helper (safe to keep)
export const config = {
  baseUrl: import.meta.env.VITE_BACKEND_URL,
};
